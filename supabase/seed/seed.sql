-- Create a test user in auth.users (requires interacting with Supabase Auth API usually, 
-- but for local dev we can mock or insert directly if needed. 
-- In practice, it's better to sign up via the UI in local dev.)

-- We'll leave this empty for now, as the best way to seed is to sign up a user via the UI,
-- then run a script to attach a business.
